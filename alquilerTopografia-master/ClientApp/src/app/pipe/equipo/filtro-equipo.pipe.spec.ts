import { FiltroEquipoPipe } from './filtro-equipo.pipe';

describe('FiltroEquipoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEquipoPipe();
    expect(pipe).toBeTruthy();
  });
});
